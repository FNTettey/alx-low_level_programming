#ifndef MAIN_H
#define MAIN_H
int _putchar(char c);
unsigned int binary_to_uint(const char *b);
unsigned int _pow(unsigned int num, unsigned int n);
#include <stdlib.h>
#include <string.h>
#include <stdio.h>
#endif
