#include"main.h"
/**
 * main - check the code
 * description: Prints the alphabet in lowercase
 * Return: Always 0.
 */
int _isalpha(int c)
/**
 * description: Prints the alphabet in lowercase
 * Parameter description: a character inputed by the user
 **/
{
if (isalpha(c) != 0)
{
return (1);
}
else
{
return (0);
}
}
