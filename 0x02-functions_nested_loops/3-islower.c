#include"main.h"
/**
 * main - check the code
 * description: Prints the alphabet in lowercase
 * Return: Always 0.
 */
int _islower(int c)  
/*description: Prints the alphabet in lowercase */
{
if (islower(c) != 0)
{
return(1);
}
else
{
return(0);
}
}
