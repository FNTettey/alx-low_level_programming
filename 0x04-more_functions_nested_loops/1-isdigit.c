#include"main.h"
/**
 * main - Entry point
 * description: Checks if a character is uppercase
 * @c a character inputed by the user
 **/
int _isdigit(int c)
{
if (isdigit(c) != 0)
{
return (1);
}
else
{
return (0);
}
}
