#include"main.h"
/**
 *main - entry point
 * mul() prints the product of two numbers
 * @a inputted number
 * @b inputted number
 *Return: Always return zero(0)
 **/
int mul(int a, int b)
{
int s;
s = (a * b);
return (s);
}
