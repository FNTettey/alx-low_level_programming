#include"main.h"

/**

 * _strlen() - Entry point
 
 * description: returns the length of a string
 
 * @s points to the string we want to find the length of
 
 **/
 
int _strlen(char *s)

{

int length;

length = strlen(s);

return (length);

}